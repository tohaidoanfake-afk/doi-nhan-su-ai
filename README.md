# Đội nhân sự A.I — bản đầy đủ

**Cả bộ trong một lần tải.** Nền dựng bộ não thứ 2 của bạn, cộng các vai làm việc trên bộ não đó — clone xong là chạy được ngay, **không phải cài thêm gì**.

> ⚙️ **Kho này do máy sinh** từ các kho lẻ. Đừng sửa trực tiếp ở đây — xem `BAN-KE.md`.

---

## Cài — một lệnh

```bash
git clone https://github.com/tohaidoanfake-afk/doi-nhan-su-ai.git
```

Mở công cụ AI **ngay trong thư mục vừa clone**, nhắn `bắt đầu`. Gõ `/` sẽ thấy đủ 7 lệnh.

⚠️ **Mở đúng thư mục `doi-nhan-su-ai`, đừng mở thư mục cha.** Mở sai thì luật vẫn nạp nhưng skill thì không — hỏng mà không có dấu hiệu gì. Chi tiết ở `README` mục *Chỗ hay sai nhất* trong `BAT-DAU-TU-DAY.md`.

## Có gì trong này

| Phần | Lệnh |
|---|---|
| 🧑‍🏫 **Thủ thư** *(nền)* | `/kiem-chung` · `/nap-kho` · `/onboard` |
| ✍️ Content | `/viet-content` |
| 🎛️ Điều phối | `/banh-xe-cuoc-doi` · `/kham-benh` · `/tong-giam-doc` |

Đi kèm: khung 13 trang nền, lộ trình 7 việc (`BAT-DAU-TU-DAY.md`), và tài liệu cài đặt trong `reference/`.

⬜ **Chưa có trong bản này:** 💰 Bán hàng — đang đóng gói.

## Bản gộp khác bản lẻ ở đâu

Cùng một nội dung, khác cách cài:

| | Bản lẻ *(`creator-ceo`)* | **Bản gộp này** |
|---|---|---|
| Cài | clone nền, rồi `claude plugin install` từng vai | **clone một lần, xong** |
| Hợp với | chỉ cần một vai | cần cả đội |
| Cập nhật | `git pull` + `claude plugin update` từng vai | `git pull` |

Muốn cài lẻ từng vai thì xem `creator-ceo/nhan-su-thu-thu` và các kho `nhan-su-*` cạnh nó.

## Cập nhật mà không mất bộ não

Bộ não của bạn nằm ở thư mục `SecondBrain/` do AI tạo ra, **không nằm trong kho này** — nên `git pull` không bao giờ đụng vào nó. Luật đầy đủ ở `README` gốc của nền, mục *Cập nhật bộ khung mà không mất bộ não*.

---

## Ai làm bộ này

**Tô Hải Đoàn** — người làm nội dung và xây thương hiệu cá nhân tại Việt Nam. Đây là quy trình tôi dùng cho công việc của chính mình mỗi ngày, đóng gói lại để bạn chạy được trên dữ liệu của bạn.

**Kẹt ở đâu thì nhắn tôi:** [facebook.com/tohaidoan](https://www.facebook.com/tohaidoan/)

Giấy phép MIT.
